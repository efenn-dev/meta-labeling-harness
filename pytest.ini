[pytest]
# The runnable test suite lives in tests/. reference/ contains the
# engine-dependent corpus builder and is not collected (it imports a private
# backtest engine that isn't part of this repo).
testpaths = tests
addopts = --ignore=reference
